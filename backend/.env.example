# Path to Firebase service account JSON key file
# Get this from Firebase Console > Project Settings > Service accounts
GOOGLE_APPLICATION_CREDENTIALS=

# Alternative: paste the JSON content directly (mutually exclusive with the above)
# FIREBASE_SERVICE_ACCOUNT_JSON=

# Firebase project ID (auto-detected from service account if not set)
# FIREBASE_PROJECT_ID=

# Server port
PORT=4000
